apiVersion: v1
kind: Service
metadata:
  name: nginxsvc2
spec:
  type: NodePort
  selector:
    type: test-deployment2
  ports:
      # By default and for convenience, the `targetPort` is set to the same value as the `port` field.
    - port: 8080
      targetPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-test2
  labels:
    type: test-deployment2
spec:
  replicas: 1
  selector:
    matchLabels:
      type: test-deployment2
  template:
    metadata:
      labels:
        type: test-deployment2
    spec:
      containers:
      - image: piotrzan/nginx-demo:blue
        name: nginx-test
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 250m
            memory: 256Mi